#include <iostream>

using namespace std;

int main()
{
	string input = "hello!";
	vector<char> test(input.begin(), input.end());

	vector<char>::const iterator itr;

	for(itr = test.begin() ; itr != test.end() ; itr = itr.next())
	{
		cout << test.itr << "\n";
	}

	return 0;
}
